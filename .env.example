# SameAsk — local / server env (never commit real keys)

# Live compare (optional server-side fallback if user doesn't paste BYOK)
OPENROUTER_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=

# Feedback chat (server-only — preferred)
SARVAM_API_KEY=
# Optional override: sarvam-30b | sarvam-105b
SARVAM_MODEL=sarvam-30b

# Other free-tier keys you can add later for Live / feedback fallbacks
GROQ_API_KEY=
